# Frontend configuration.
# The AI pipeline + Azure key now live in the backend service (see server/.env.example).
# Point the frontend at that backend:
VITE_API_BASE_URL=http://localhost:8080

# Passcode for the frontend lock screen (not authentication — a shared workspace lock).
# Defaults to "esource" when unset.
VITE_APP_PASSCODE=esource
